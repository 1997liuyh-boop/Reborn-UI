# 文档站页面技术债清单

> 检索日期：2026-08-11
> 检索范围：文档站页面层（布局 / 文档组件 / 演示区 / 样式与全局配置），不含组件库本体
> 结论：代码可正常运行，但**同一事实存在多份手工同步的副本**是最大债务；另有 4 处会产生真实偶发 bug 的泄漏/竞态，2 处 postMessage 安全松动。
>
> **2026-08-13 补充**：新增全仓审查章节（安全事故 / 仓库卫生 / 组件库本体 / content 文档层 / 文档站新发现），见文末「2026-08-13 全仓审查补充」。与上文重复的条目不再重写。

---

## P0 — 会产生真实 bug，优先修复

### 1. 锚点滚动逻辑双份实现 + window 全局魔法标记

- `plugins/anchor-scroll.client.ts:3-13` 与 `app/router.options.ts:4-16`：`MAIN_HEADER_HEIGHT` / `SUB_HEADER_HEIGHT` / `ANCHOR_GAP` 常量及 `getAnchorScrollOffset()` 逐字复制；hash 百分号解码、`prefers-reduced-motion` 回退判定也各写一遍。
- 跨模块通信靠 `window.__REBORN_SKIP_NEXT_HASH_SCROLL__`（写入 `anchor-scroll.client.ts:100`，读取 `router.options.ts:25-27`）。若 `router.push` 被中止或 `scrollBehavior` 未触发，标记永久残留，下次同 hash 导航被静默吞掉。
- `anchor-scroll.client.ts:83-107` 的 `document.addEventListener('click', ..., true)` **永不移除**，且捕获阶段 `stopPropagation()` 会吞掉所有组件级 hash 链接的 click 处理器（`DocsFloatingToc.vue:10-13` 的注释已把该副作用当成设计约定）。

**建议**：抽 `app/utils/anchorScroll.ts` 统一常量与工具函数；魔法标记改为模块级共享变量；监听器在 `app:unmounted` 注销，`stopPropagation` 降级为 `preventDefault`。

### 2. 滚动偏移三处数值互不相同，且 JS/CSS 双源真相

- `DocsFloatingToc.vue:68` 用 `160`；`router.options.ts:15` 与 `anchor-scroll.client.ts:13` 算出 `136`。点击目录跳转后高亮的章节可能不是刚跳到的那条。
- JS 侧硬编码 `64+64+8`，CSS 侧用 `var(--ui-header-height)`（`docs-floating-toc.config.ts:15`、`DocsMobilePanel.vue:168`、`app.config.ts:38,48`）。改头部高度只会改到 CSS 一侧，锚点偏移立刻失准。

**建议**：三处共用同一 `getAnchorScrollOffset()`；JS 侧运行时读取 CSS 变量，消除硬编码。

### 3. 生命周期泄漏（三处确切）

- `DocsPage.vue:98-110`：MutationObserver 在 `nextTick` 异步回调里才创建。若组件在 nextTick 前卸载，`onUnmounted` 执行时 `tocObserver` 仍为 `null`，随后创建的 observer 永不 `disconnect()`。
- `DocsFloatingToc.vue:83-96`：`requestAnimationFrame(updateActive)` 未保存句柄，卸载后仍会执行并访问 DOM。
- `DocsMobilePanel.vue:155`：`setTimeout(syncTheme, 600)` 无句柄无清理，卸载后仍向已销毁的 iframe postMessage。

**建议**：保存句柄，在 `onUnmounted` / `onScopeDispose` 中取消；observer 创建加卸载守卫。

### 4. demo 注册状态存在三条清理路径互相竞态

- 注册：`ComponentPlayground.vue:14-17`（setup 顶层无条件调用）
- 自动注销：`useUniDemoPanel.ts:63-66`（`onUnmounted`）
- 路由清空：`DocsMobilePanel.vue:41-44`（`watch(route.path, clear)`，清理逻辑写在消费端组件，职责倒置）

路由切换时若新页组件先注册、旧页 watch 后触发 `clear()`，新页 demo 会被误清空。

**建议**：只保留 `onUnmounted` 自动注销一条路径，删除路由级 `clear()`。

### 5. ComponentPlayground 兜底分支是死代码且产出非法 URL

`ComponentPlayground.vue:38-42, 62-66`：`props.url` 为空时 `computedUrl` 拼出 `"/undefined"`，永远为真值，`v-else` 的 "No Preview URL provided" 永不渲染，DeviceFrame 会加载 404 地址。

**建议**：`computedUrl` 在 `!props.url` 时返回 `null`。

### 6. postMessage 信道两端都松

- 发送端 `DeviceFrame.vue:22-25`、`DocsMobilePanel.vue:149`：targetOrigin 用 `'*'` 通配。
- 接收端 `packages/uniapp-project/src/App.vue:4-14`：不校验 `event.origin`，任何嵌入该 H5 的第三方页面都可注入 `{type:'theme-change'}`；`onLaunch` 注册的监听器永不移除。

**建议**：两端都锁定 origin；接收端卸载时解绑。

### 7. Playground 异步 `hide()` 竞态 + visibilityMap 只增不减

`Playground.vue:77-100`：`updateVisibility` 无版本号/取消机制，`modelValue` 快速变化时旧 Promise 后 resolve 会覆盖新结果；`controls` 变更后旧 key 永久残留。

**建议**：引入递增 token 丢弃过期结果，每轮重建 map。

### 8. DemoStage 直读 iframe contentDocument 无防御

`DemoStage.vue:85-89`：直接访问 `frameRef.value?.contentDocument` 未做 try/catch。一旦 `baseURL` 配置到异源，会抛 SecurityError 且无降级路径。

**建议**：包 try/catch，异源时回退 postMessage 协议（与 DeviceFrame 统一）。

---

## P1 — 单一事实多份副本（改一处必漏一处）

| 事实 | 副本数 | 位置 | 建议 |
|---|---|---|---|
| 面板宽度 420px | 4 | `DocsMobilePanel.vue:168`（`w-[420px]`）、`docs.vue:46`（`2xl:pr-[420px]`）、`docs-floating-toc.config.ts:39`（手算 `27rem`）、`DocsMobilePanel.vue:63`（派生值 384） | 定义 `--docs-panel-width` CSS 变量，四处改用 `calc()` |
| 断点 1536px | 3+ | `ComponentPlayground.vue:24`、`DocsMobilePanel.vue:90`（注释明说"靠人肉保持一致"）、模板 `2xl:` 类双轨表达 | 抽 `useDocsBreakpoints()` composable |
| 断点 1024px | 2+ | `router.options.ts:14`、`anchor-scroll.client.ts:12`，与 `lg:` 类双轨 | 同上 |
| 机型清单 + `Device` 接口 | 2 | `DocsMobilePanel.vue:21-34` 与 `DeviceFrame.vue:41-54`，已漂移（5 项 vs 6 项含 iPad） | 抽 `app/constants/devices.ts` |
| iframe 主题同步协议 | 3 | `DeviceFrame.vue:18-31`（postMessage）、`DocsMobilePanel.vue:148-161`（postMessage + 600ms 补发）、`DemoStage.vue:85-91`（直写 contentDocument） | 抽 `useIframeThemeSync(frameRef)` composable |
| baseURL 拼接 | 3 | `ComponentPlayground.vue:38-42`、`DemoStage.vue:75-78`、`useUniDemoPanel.ts:27-31`（均为 `(app.baseURL \|\| '/').replace(/\/$/, '')`） | 抽公共工具或用 `withBase()` |
| `TocLinkItem` 接口 | 2 | `DocsPage.vue:50-55` 与 `DocsFloatingToc.vue:18-23` 完全相同 | 提到 `app/types/toc.ts` |
| viewport 尺寸 375/768 | 3 | `demo-stage.config.ts:34,38` 类名、`DemoStage.vue:59-60` label 文案、`:65-67` sizeHint 返回值 | 单一常量派生 |
| 手机壳样式/加载占位 | 2 | `DocsMobilePanel.vue:207,226-231` 与 `DeviceFrame.vue:70,77-80` 逐字重复 | 抽共享 `DeviceShell` 展示组件 |
| demo-stage 画布类串 | 2 | `demo-stage.config.ts:33,37` 仅高度一处差异，其余全量复制 | 提取 `canvasBase`，变体只覆盖高度 |

---

## P1 — 主题 / 样式层系统性冲突

### 9. 两套色彩体系打架

`app/assets/theme/base.css:86-89` 的自定义 `--color-inverted/default/elevated/accented` 会生成与 Nuxt UI 同名的 `.bg-default` / `.text-inverted` 等工具类（`app.config.ts:12,38` 正在使用），谁生效取决于 CSS 生成顺序，非常脆弱。
**建议**：自定义语义色改用独立前缀（如 `--color-rb-*`）。

### 10. 中文字体回退失效

`--font-sans` 定义两次：`base.css:75`（PingFang/微软雅黑/Noto Sans SC 栈）被 `reborn-ui.css:9`（`"Geist", sans-serif`）静默覆盖，中文回退栈实际完全失效。
**建议**：合并为一处，中文回退接到 Geist 之后。

### 11. gray 色阶双轨

`base.css:53-62` 自定义 `--color-gray-1..10`，与 Tailwind 默认 `gray-50..950` 全仓混用（260 处 / 62 文件，如 `reborn-select-trigger.config.ts:14` 用 `gray-2/gray-7`，registry json 用 `gray-50/gray-700`）。
**建议**：自定义色阶换名（如 `neutral-1..10`），禁止与内置刻度同名。

### 12. 主色定义自相矛盾

`base.css:78,83`：`primary` 与 `error` 同为 `var(--color-red-6)`；`reborn-ui.css:10,16` 又把 `--ui-primary` 设为 black/white。主色到底是红还是黑，两个文件答案相反。
**建议**：统一主色来源，`error` 用独立红。

### 13. 暗色模式缺口 + 三套处理方式并存

- `base.css:78-89` 的语义 token **没有任何 `.dark` 变体**，而 `nuxt.config.ts:57-60` 默认 `preference: "dark"`——默认态跑在未适配的 token 上。
- 三套方式并存：变量覆盖（`reborn-ui.css:13-17`）、独立 utility（`pattern-background-d`）、组件内散落 `dark:` 类。

**建议**：语义 token 统一走 `.dark` 变量覆盖，组件层不再写 `dark:` 颜色类。

### 14. z-index 无统一刻度

注释约定 header 50 > 悬浮目录 40 > 面板 30 > Tab 20（`docs-floating-toc.config.ts:7`、`DocsMobilePanel.vue:17`、`ComponentTabs.vue:106`），实际裸值遍地：`app.config.ts:38` 的 `z-9`、`DeviceFrame.vue:63` 的 `z-[999]`、`reborn-context-menu.config.ts:13,30` 的 `z-[9999]/z-[10000]`、`LiquidGlassDemo.vue:15` 的 `z-999999` 等十余种。
**建议**：base.css 落一组 `--z-header/--z-overlay/--z-popover/--z-toast` 语义层级。

### 15. 硬编码颜色未走 token

`reborn-ui.css:20-28`（选区色手写 zinc 值）、`:31-52`（滚动条三个近似 rgba 无变量）、`:70-72,89-91`（`#09090b` 与第 15 行 `--ui-bg` 同值抄两遍）。
**建议**：抽 `--rb-scrollbar-thumb`、`--rb-selection-bg` 等变量，暗色只覆盖变量。

### 16. 断点与媒体查询不一致

`DirectionAwareHover.vue:251` 手写 `@media (max-width: 768px)`，与 Tailwind `md`（min-width:768px）在 768px 处双规则重叠命中。

---

## P2 — 类型与规范债

### 17. `any` 重灾区

- `useOverlay.ts`：19 处 `any`（props、resolve、close 值全无类型）
- `propsToControls.ts`、`useFieldGroup.ts`（`inject<any>('rebornForm')` 等）
- `Playground.vue:21,25,38,65`：props / defaultValue / hide 回调 / modelValue 全 `any`，控制台完全丧失类型推导
- `DocsPage.vue:24`：`page: Record<string, any>`，加 `:193,198` 两处 `as any` 断言（建议改用 `@nuxt/content` 生成的集合类型）

### 18. 违反项目编码规范

- `Playground.vue:60-67`：用 `defineEmits('update:modelValue')` + 手动 `updateField`，**未使用 `defineModel`**（规范硬性要求；对照 `DemoStage.vue:51` 是合规写法）
- `ComponentPlayground.vue:4-7`、`DeviceFrame.vue:4-6`：内联匿名 `defineProps<{...}>()`，未用接口式
- 英文注释：`DeviceFrame.vue:8,13,21`、`ComponentTabs.vue:70,91`、`nuxt.config.ts:17,88`（项目要求全中文）
- 英文 UI 文案：`ComponentPlayground.vue:64,74-75,84,88-89`、`ComponentTabs.vue:133-134,143,152-154`、`DocsPage.vue:39-43`（'Edit this page' 等），且 `DocsFloatingToc.vue:35` 的 'On This Page' 与 `DocsPage.vue:42` 的 'Table of Contents' 是同一目录的两个不同默认标题
- `Playground.vue:9`、`DemoStage.vue` 的 `tv` 来源不一致：前者直连 `tailwind-variants` 绕过 `app/lib/tv.ts` 的 `twMergeConfig` 封装（全仓 57 文件仅此一处直连，自定义类 `rounded-ui-base` 可能被 tailwind-merge 误删）
- `Playground.vue:149`：`tv(config)` 写在 computed 内部，每次变化重新构造实例

### 19. 命名混乱

- `component-playground/ComponentPlayground.vue` 与 `play-ground/Playground.vue` **实为嵌套关系**（外层画布壳 146 处引用 / 内层 props 控制台约 20 处引用），并非重复废弃，但目录拆词、组件名、页面 UI 都自称 Playground，无法分辨。建议重命名 `DemoShell` / `PropsPlayground`。
- `DocsMobilePanel` 名为"移动端面板"，实为仅 ≥1536px 桌面宽屏才显示的移动端预览器，名实相反。建议 `DocsDevicePreviewPanel`。
- 同一状态三个名字：`hasDemos`（`useUniDemoPanel.ts:88`）→ `hasMobilePanel`（`DocsPage.vue:47`）→ prop `inset`（`DocsFloatingToc.vue:31`）。
- `Playground.vue:74`：注释"显回状态管理"系"显隐"错别字；`visibilityMap` 存的是"是否隐藏"，语义相反，建议改名 `hiddenMap`。
- `isRootPage.ts:9`：用 `fullPath` 精确匹配，URL 含 query/hash 即判定失效。建议改 `route.path`。

### 20. 代码风格分裂

- 双引号+分号（`docs.vue`、`router.options.ts`、`DocsMobilePanel.vue` 等）vs 单引号+无分号（`DocsPage.vue`、`DocsFloatingToc.vue` 等），部分文件内部自相矛盾；缩进 2/4 空格混用。
- 插件目录双轨：`nuxt.config.ts:45-50` 手工注册根目录 `plugins/`，同时 `app/plugins/loading.ts` 走自动注册。

**建议**：ESLint stylistic 规则统一后全量 `--fix`；插件全部迁到 `app/plugins/`。

---

## P3 — 死代码与冗余

| 项 | 位置 | 说明 |
|---|---|---|
| 整文件死代码 | `app/assets/css/main.css` | 全仓无引用（nuxt.config 只注册了 reborn-ui.css） |
| 死样式 | `reborn-ui.css:58-62, 79-84` | `pattern-background-l1/d1` 旧粉蓝渐变遗留，无使用处 |
| 重复定义 | `reborn-ui.css:99, 153` | `--animate-aurora` 同文件定义两次 |
| 三重定义 | `base.css:107-113`、`reborn-ui.css:156-162`、`TrackDemo.vue:188-193` | `scrollbar-hide` 逐字重复三份 |
| 注释掉的代码 | `docs.vue:48`、`nuxt.config.ts:103-111` | 靠 git 记历史，应删除 |
| 恒等 computed | `DeviceFrame.vue:14-16` | `iframeSrc` 只是 `props.src` 透传 |
| 残废函数 | `Playground.vue:145-147` | `getAccentColor(_index)` 恒定返回同一类名 |
| 冗余调用 | `Playground.vue:106-108` | `onMounted(updateVisibility)` 与 `immediate: true` 的 watch 重复执行 |
| 不可达分支 | `router.options.ts:34-37` | `scrollBehavior` 仅客户端执行，SSR 分支永不进入 |
| YAGNI 预留 | `DocsMobilePanel.vue:193-201`、`useUniDemoPanel.ts:71-73` | 多 demo 切换 chips 与 `setActive`，当前每页仅一个 demo |
| 无消费者导出 | `useUniDemoPanel.ts:90` | `activeId` 全项目无人使用 |
| 低覆盖率抽屉 | `ComponentPlayground.vue:72-100` | Customize 抽屉全仓仅 3 个 config 使用，146 个消费者共同承担开销 |
| 疑似无效类名 | `DeviceFrame.vue:79` | `text-red-5` 非标准 Tailwind 色阶写法，疑似 UnoCSS 残留 |

## P3 — 其他

- **触屏交互死角**：`DocsFloatingToc.vue:103-105` 展开靠 `mouseenter`，关闭只有 `mouseleave`，触屏点开后无法关闭。建议补 `onClickOutside` + Esc。
- **文档硬编码文件清单**：`content/2.components/input-and-forms/reborn-select.md:16` 单行硬编码 11 个文件名，解析链纯运行时字符串拼接（`ComponentTabs.vue:115` → `DemoStage.vue:71,77` → `ComponentLoader.vue:73` 的 glob），组件重命名会静默 404；且与 `packages/cli/registry/components/reborn-select.json` 的 `files[]` 双写不一致。建议以 registry 为单一数据源。
- **SSR 守卫靠时机而非显式判断**：`DocsMobilePanel.vue:79,120`、`DocsFloatingToc.vue:70,99` 裸用 `window/document/fetch`，仅靠"在客户端回调里"隐式保证。建议函数入口加 `if (!import.meta.client) return`。
- **`useState` 误用**：`DocsMobilePanel.vue:70` 的 `uni-render-available` 号称会话级缓存，实际每次 SSR 重置且仅单消费者，改模块级 `ref` 即可。
- **深度监听静态配置**：`Playground.vue:104` 对含函数与数组的整棵 `controls` 树做 `deep: true` 监听，成本高且配置几乎不变。
- **代码生成器脆弱**：`Playground.vue:111-142` 无条件硬塞 `v-model`（展示型组件并无 v-model）；对象值单引号包 `JSON.stringify`，值含单引号即产出非法模板；`!==` 比较使对象/数组默认值恒判"已修改"。

---

## 建议偿还顺序

1. **修 bug 类（P0 #1-#8）**：锚点滚动抽公共模块、移除 window 魔法标记、三处泄漏加清理、demo 注册收敛单一路径、computedUrl 空值处理、postMessage 锁 origin——一次小型 PR 可覆盖。
2. **收敛单一真相（P1 副本表）**：`--docs-panel-width` 变量、`constants/devices.ts`、`useIframeThemeSync()`、断点 composable。
3. **主题层专项（P1 #9-#16)**：解决双色彩体系、字体栈、primary 定义冲突，落 `--z-*` 语义层级。
4. **规范清扫（P2/P3）**：删死代码、英文注释与文案中文化、`defineModel` 改造，ESLint 统一风格后全量 fix。

---
---

# 2026-08-13 全仓审查补充

> 检索范围：仓库卫生与工程配置、组件库本体（`app/components/reborn/`）、content 文档层、文档站新发现
> 与 2026-08-11 清单重复的条目（字体栈、色彩撞名、锚点双份实现、生命周期泄漏、clear 竞态、Playground 各项等）不再重写，仅记录新增发现。

## S0 — 安全事故（优先级高于本清单一切条目）

### S1. SSH 私钥明文提交并已推送到全部远端

- `.cnb/secrets/deploy.yml`：完整的 4096 位 OpenSSH RSA 私钥（注释 `cnb-deploy`）明文在库，**当前 HEAD 仍存在**，非历史遗留。
- `.cnb.yml` 配套明文：`SERVER_IP: "110.42.242.107"`、`SERVER_USER: "root"`、`DEPLOY_PATH: "/www/wwwroot/re-ui"`——持有仓库读权限即可 root 登录生产服务器。
- 已扩散到 cnb / gitlab / origin 至少 3 个远端的多个分支历史（引入提交 `d52ff85`，修改提交 `6069687`）。
- `.gitignore:46` 的 `.cnb/secrets/` 规则无效，因为文件在规则加入前已被跟踪——gitignore 对已跟踪文件不生效。

**处置顺序（前两步不可省略，密钥吊销前任何 git 清理无意义）**：
1. 立即在 110.42.242.107 上从 `/root/.ssh/authorized_keys` 移除该公钥并轮换新密钥对；
2. 新密钥改存 CNB 加密变量/密钥仓库，`.cnb.yml` 通过 imports 引用；
3. `git rm --cached .cnb/secrets/deploy.yml`，用 `git filter-repo` / BFG 清洗全部分支历史并强推（协调协作者重新 clone）；
4. `SERVER_IP` / `DEPLOY_PATH` 一并移入 CI 变量。

⚠️ 本文件在密钥轮换完成前不应推送到公开仓库（含服务器 IP 与文件路径）。

---

## S1 — 仓库卫生

| 问题 | 证据 | 建议 |
|---|---|---|
| `.claude/settings.local.json` 被跟踪 | 历史含 6 次提交；`.gitignore:79` 规则对已跟踪文件无效 | `git rm --cached` 后规则才生效 |
| `.gitignore` 意图自相矛盾 | 第 50 行忽略 `.cnb.yml`，第 61 行注释却说"应保留在仓库中" | 明确取舍，删一处 |
| CLAUDE.md 与 AGENTS.md 逐字节相同 | md5 一致，且 100% 为 GitNexus 生成样板，无任何项目实质信息（构建命令、monorepo 结构、组件约定全缺） | AGENTS.md 为唯一真源，CLAUDE.md 只留引用；在生成标记块外补手写内容 |
| `.agent/` 与 `.agents/` 双目录都被跟踪 | `.agent/skills/` 三套文件与 `.agents/` 完全重复 | 删除 `.agent/`（单数，疑似笔误副本） |
| 根目录垃圾目录 `3001/` | 内含空 `node_modules`，疑似误敲命令产物 | 直接删除 |
| 根目录散落文档 | `技术债.md`、`impel-ui.md`、`本地发包.md` | 收拢到 `docs/`；中文文件名建议改 ASCII（本文件建议改 `docs/TECH-DEBT.md`） |
| `.claude/skills/` 未提交 | CLAUDE.md 表格引用了其中 6 个路径，不提交则引用对协作者全部失效 | 提交（`.gitignore` 已正确只忽略 local 文件） |
| 缺 `.env.example` | `.gitignore:25` 已有 `!.env.example` 白名单但文件不存在 | 补一个占位示例 |

## S1 — 工程配置（package.json / nuxt.config / 提交规范）

### S2. 依赖问题

- **`eslint` 同时在 dependencies（`^9.39.2`）与 devDependencies（`^9.37.0`），版本不一致**；`@nuxt/eslint` 同样重复（`1.12.1` vs `^1.9.0`）。只保留 devDependencies 一份。
- `@nuxt/kit`、`@oxc-parser/wasm`、`tailwindcss`、`zod-to-json-schema`、`json5`、`scule` 等构建期包误入 dependencies，应下沉。
- 版本约束风格混乱：精确锁定与 caret 混用无规律；有 lockfile 的前提下建议统一 caret。
- `zod` 停留 v3 且靠 `pnpm.overrides` 强压冲突，需排查来源并规划升级。
- `.npmrc` 硬编码 npmmirror 镜像源，强制所有协作者（含海外 CI）使用，建议移到本地/CI 配置。

### S3. 脚本与质量基建缺口

- **零测试**：146+ 组件无 test 脚本、无测试框架。建议 Vitest + @vue/test-utils 起步。
- **无 typecheck 入口**：`vue-tsc` 已装但 scripts 无调用。加 `"typecheck": "vue-tsc --noEmit"`。
- `preview` 脚本用 Unix 语法 `PORT=3333 node ...`，Windows（主开发环境）直接失败，需 `cross-env`。
- git hooks 未落地：eslint 配置 ignores 写了 `.husky/` 但目录不存在。建议 simple-git-hooks + lint-staged + commitlint。
- 无 stylelint，而 CSS 重复定义问题已被本清单多处记录，可自动拦截。

### S4. nuxt.config / app.config

- `runtimeConfig.public` 键名用了 `NUXT_` 保留前缀（`nuxt.config.ts:86-87` 的 `NUXT_CLARITY_ID` 等），造成 `NUXT_PUBLIC_NUXT_CLARITY_ID` 诡异覆盖路径，应改 `clarityId` / `adsenseAccount`。
- `nuxt.config.ts:89` `SITE_URL` 双重回退（`NUXT_PUBLIC_SITE_URL || SITE_URL`），与 `:93` 硬编码的 `llms.domain` 三处易漂移，统一单一来源。
- `nuxt.config.ts:138` `nitro.publicAssets` 指向 `packages/uniapp-project/dist/build/h5`——裸跑 `pnpm dev` 时该目录不存在，是隐式陷阱，README 应注明。
- **`app.config.ts` 社区链接指向不存在的 `github.com/unovue/reborn-ui`**（实际托管 gitlab.rebornnet.cn），疑似模板残留，会把用户导向错误位置。

### S5. 提交规范

- 伪 scope：`feat: web端：xxx` 应写 `feat(web):`，否则 changelog 工具无法解析分组。
- merge 噪音占最近 60 条的 27%（`git pull` 未 rebase 产物），建议 `git config pull.rebase true`。
- 空洞信息存量：`feat: 修改`（正是引入私钥的那条）、`编辑文件 deploy.yml`。commitlint 的 `subject-min-length` 可拦截。

---

## S1 — 组件库本体（原清单未覆盖范围）

> rpx 规范结论：`app/components/reborn/ui/` 为纯 Web 端（rpx 0 次），uniapp 端在 `packages/uniapp-project/`（rpx 151 次），两端已正确分离，**rpx 不适用于本目录，不判违规**。

### S6. defineModel 违规（规范硬性要求）

- `reborn-select/RebornSelect.vue`：完全未用 defineModel——`:33` 声明 `modelValue`、`:254,258,271` 三处手写 `emit("update:modelValue")`、15 处直读 `props.modelValue`。
- `reborn-fab/RebornFab.vue`：完全未用 defineModel，且 `:90` 是**全库唯一运行时数组式无类型 emits**；`:93-124` "ref + watch 回灌 + emit" 三段式手动同步可整体替换。
- `reborn-cascader/RebornCascader.vue:120,125`：defineModel 与 `update:modelValue` emits 声明**同时存在**，后者冗余，删除。
- 运行时对象式 defineProps 残留 7 个装饰组件（ContainerScrollTitle/Card、Meteors、ImagesSlider、RadiantText、TracingBeam、**RebornFireworks**——唯一 reborn 前缀，优先改）。

### S7. 死代码（正确性风险）

`RebornCascader.vue:301-303`：`return` 之后永不可达的 `activePath.value = selectedPath.value.slice()`，位于非叶子节点核心交互路径，且上方还留着描述该不可达逻辑的注释。须清理并确认交互意图。

### S8. 注释违规与缺失

- 英文注释 5 处：`RebornSelectDate.vue:139`、`RebornTimePanel.vue:128,133`；英文错误文案 2 处：`RebornSelectTrigger.vue:305`、`RebornImage.vue:255`。
- **约 40 个 props 完全无注释**（比英文注释更影响文档生成）：`RebornSelectDate.vue:26-37`（12 个）、`RebornSelectTrigger.vue:12-32`（11 个）、`RebornTimePicker.vue:41-56`（16 个）。
- 存量英文注释密集区（后续排期）：`DocsAsideLeftBody.vue`（19 处，见 S13）、`MultiStepLoaderDemo.vue`（9 处）、`reborn-qrcode/qrcode.ts`（40+ 处，第三方移植，建议标注来源后豁免）。

### S9. any 滥用

- `RebornFab.vue:204-215`：8 个 UI 工厂全为 `(opts?: any)`，同库其他组件均为 `(opts?: { class?: any })`；`:234` 同行两次 `as any`，应 `keyof typeof` 收敛。
- Select / Cascader / SelectDate 的选项值与 modelValue 全链路 any，建议泛型 `<T = string | number>`。
- `RebornImage.vue:154,161,189,204`：已 import `CSSProperties` 却写 `style: any`；`onLoad/onError(e: any)` 应为 `Event`。
- `RebornDropdownItem.vue:38-40`：`as any` + 双形态兜底解包，根因是 provide/inject 契约未定清，应在 provide 侧统一解包。

### S10. 超长组件

本次修改文件中：`RebornFab.vue` **999 行**（拖拽/吸边/peek/环形布局/胶囊测宽 5 套机制耦合，建议抽 `useFabDrag` / `useFabPeek`）、`RebornImage.vue` 643 行（放大镜几何约 300 行可抽 `useImageMagnifier`）、`RebornTimePanel.vue` 635 行。
全目录超 500 行共 13 个，最大：`RebornCarousel.vue` 1912、`RebornTransfer.vue` 1528、`RebornDatePickerPanel.vue` 1032。

### S11. 样板重复与魔法数

- **UI 工厂样板逐字段重复 60+ 次**（Cascader 11 / SelectDate 12 / SelectTrigger 9 / Select 8 / Image 10 / Dropdown 8 字段），可抽 `createUiProxy(styles, overrides)` 一次消除数百行。
- config 硬编码尺寸：`reborn-cascader.config.ts:15-35` 五处 `min/max-[...]px` 散落三档 size；`reborn-fab.config.ts:112-118` 相同 12px 偏移重复 4 次、`:49` 硬编码阴影色 `#cfcece` 未走主题。
- JS 魔法数：`RebornFab.vue:127` 的 `400`（与 Tailwind duration 隐式耦合）、`:648` `safeZone = 140`、`:685` `320/160`；`RebornCascader.vue:274,322` 两个语义不同的裸延时 `100/150`。参照 `RebornSelectTrigger.vue:90-94` 具名常量的正确范例。

### S12. console 残留

- 组件库内 5 处（`RebornSelectTrigger.vue:305`、`RebornImage.vue:255`、`RebornForm.vue:105`、`DitherShader.vue:330,369`），应统一静默降级或 logger。
- examples 内 28 处 `console.log`，发布后污染文档站控制台，密集区：RebornDialogDemo（6）、RebornTransferDemo / RebornMenuDemo / RebornSearchBoxDemo / RebornDraggableDemo（各 3）。

---

## S1 — 文档站新发现（2026-08-11 清单未记录）

### S13. `hasDemos` 的 SSR 注释是错的，实际产生水合差异

`docs.vue:33-34` 注释断言"SSR 期即注册、首帧一致"，但注册链路经 `ComponentViewer.vue:28` 的 `<ClientOnly>`——`register()` 永不在服务端执行。SSR 首帧 `hasDemos === false`（无 `2xl:pr-[420px]`），客户端挂载后突变为 `true` → 布局跳动 + 潜在 attribute mismatch，`DocsPage.vue:202` 的 `:inset` 同理。修法：删除错误注释，明确其为纯客户端状态并做客户端过渡；或 url 提到 frontmatter 由页面级 SSR 注册。

### S14. `DocsMobilePanel` 视口回弹后 iframe 显示错误 demo

`DocsMobilePanel.vue:108-132` 切换 demo 只更新 `shownUrl` 不更新 `initialUrl`；`:233` 的 `v-if` 在窗口缩窄时销毁 iframe，回弹重建时 `:src="initialUrl"` 仍是最早注册的 demo，而 `shownUrl` 声称是当前的，守卫 `url === shownUrl.value` 还会阻止修正。修法：iframe 卸载时同步复位两个 url。

### S15. `DocsFloatingToc` 性能与可访问性

- `:66-81` 每帧 rAF 对**所有**标题 `getElementById` + `getBoundingClientRect()`，长文档每帧强制同步布局，应改 `IntersectionObserver`。
- `:103-110` 刻度条为裸 `<div @click>`，无 `role` / `tabindex` / 键盘支持（触屏问题原清单 P3 已记录，键盘可访问性为新增）。

### S16. `DocsPage` DOM 兜底目录成本

- `:83` 每次去抖回调用 `JSON.stringify` 序列化整棵目录树做等值比较。
- `:101-109` MutationObserver `subtree: true` 监听整个正文，含 marquee/meteors 等常驻动画的页面会以 300ms 节奏持续全量扫描 + 序列化。根因是 MDC 插槽标题不进构建期 TOC，应在 content transformer 构建期解决。

### S17. `DocsAsideLeftBody.vue` — 单文件问题最密集

- 19 处英文注释（`:17,26,33,43,48,56,59,68,73,115,124,127,147,182,187,215,222,239,254`）。
- 疑似死分支：`:27` 依赖 `navigation.fields` 配置（已被注释掉），`tags` 字段不会存在，硬编码的 `[{label:'New'},{label:'UniApp'}]` 大概率永不执行。
- `:126` 裸表达式 `element.offsetHeight;` 触发 reflow，压缩器可能移除，改 `void element.offsetHeight`。
- `:163` 对整棵导航树 `deep: true` watch，而源是每次重算的 computed。
- `:107` `overflowY === "overlay"` 为已废弃 CSS 值；`:199,207` 硬编码色值与 sparkles 参数。

### S18. content 层：schema 与数据脱节、取值混乱

- `content.config.ts:11-13` 要求 `category`/`tags` 必填、`badge` 限定 `New|Updated`；实际 36 个 md 无 `tags`、3 处 `badge: Update`（非法值）；被真实消费的 `navigation.badges/chip` 反而不在 schema。
- frontmatter 取值混乱：`navigation.badges.label` 有 `通`×35 / `Web`×20 / `UniApp`×8 / `WEB`×3 / `web`×2 / `通用`×1 / `新`×1 七种写法；`badge: New` 与 `chip: NEW` 语义重复无分工。
- 标题格式四种并存：`按钮` / `悬浮按钮 (Fab)` / `图片 Image` / `Dropdown 下拉菜单`。
- **9 个组件缺文档**：`reborn-select-trigger`、`reborn-sku`（业务组件，值得关注）及 7 个装饰组件（部分实为组件名与 md 名不成体系无法对应，如 `card-3d` ↔ `3d-card.md`）。
- `reborn-select2.md` 指向不存在的 `RebornSelectDemo2` / `RebornSelectConfig2`，`componentFiles` 为空数组，疑似废弃残留，确认后删除。
- 建议：schema 与数据对齐后**加 CI 校验**一劳永逸。

### S19. uniapp demo URL 数据源不规范

十余个 Config 文件写 `#pages/`（缺斜杠：RebornBadge/Collapse/ColorPicker/Image/Input/LoadingConfig 等），靠 `useUniDemoPanel.ts:27-31` 的 `normalizeUniDemoUrl` 运行时兜底。应修数据源、去掉兜底（或 dev 期 `console.warn` 标记待修文件），否则 workaround 永久留存。

### S20. 杂项清理

- `AppHeader.vue:4-7,31-34`：连续 3~4 行空行，删代码残留。
- `anchor-scroll.client.ts` 同时用 `.client.ts` 后缀和 `mode: "client"`，冗余，留一个。

---

## S21 — reborn-web 规范执行核查（2026-08-13，对照 `.agents/skills/reborn-web` 硬性条款全目录机械核查）

> 结论：8 条硬性条款中 7 条存在违规，其中两条为系统性缺口。规范文档本身完善，存量代码未回改。

### S21.1 defineModel 缺口 — 20 个组件（系统性，覆盖整个表单家族）

以下组件手写 `emit("update:modelValue")` 且完全未使用 defineModel（与已合规的 16 个组件交集为空，无混用）：

> RebornFab、RebornSelect、RebornInput、RebornTextarea、RebornCheckbox、RebornCheckboxGroup、RebornSwitch、RebornSlider、RebornRadio、RebornRadioGroup、RebornRate、RebornInputNumber、RebornInputOtp、RebornSearchBox、RebornColorPicker、RebornColorPickerPanel、RebornDatePickerPanel、RebornSku、RebornOverlay、RebornDraggable

已合规的 16 个（Cascader/SelectDate/TimePicker/Carousel/Collapse/Popup/Menu 等）多为近期组件——规范系后立，存量未回改。改造注意：Checkbox/Radio 等含 group 注入逻辑，需逐个回归。

### S21.2 ClassValue 收口全库未落地 — 449 处 any / 60 文件（系统性）

`customClass?: any` / `ui?: any` / `class?: any` 共 **449 处、60 个文件**；正确用 `ClassValue` 的仅 32 个文件且多为同文件混用。重灾区：`RebornCarousel.vue`(26)、`RebornContextMenu.vue`(19)、`RebornColorPickerPanel.vue`(18)、`RebornGuide.vue`(14)、`RebornDialog.vue`/`RebornMenu.vue`(各13)。建议与 S11 的 `createUiProxy` 抽取一并落地，避免 449 处手改。

### S21.3 defineEmits 字符串数组（禁止项）— 6 处

`RebornFab.vue:90`（6 事件全无类型）、`RebornImage.vue:67`、`RebornBackTop.vue:62`、`RebornDraggable.vue:6`、`VanishingInput.vue:23`、`FlipWords.vue:15`。

### S21.4 tv 直连 tailwind-variants（禁止项）— 3 处

- `reborn-affix/reborn-affix.config.ts:1` — 双重偏离：来源错误 + config 内创建 tv 实例（其他 config 均导出纯对象）
- `reborn-main/RebornMain.vue:3`、`reborn-header/RebornHeader.vue:4`

绕过 `~/lib/tv` 的 `twMergeConfig`，`rounded-ui-*` 等自定义令牌类可能被 tailwind-merge 误删（与 P2 #18 的 Playground.vue 同类问题）。

### S21.5 defaultVariants 缺失 — 5 个 config

有 `variants` 无 `defaultVariants`：`reborn-breadcrumb`、`reborn-watermark`、`reborn-coupon`、`reborn-color-picker`、`reborn-text`（按条款字面违规，落地前逐个确认是否确无默认值需求）。

### S21.6 其他（已在 S6-S9 记录，此处仅索引）

运行时 defineProps 7 个装饰组件（S6）；`ColourfulText.vue:33` 用 `NodeJS.Timeout` 而非 `ReturnType<typeof setTimeout>`；英文注释与 40 个无注释 props（S8）；Cascader 死代码（S7）。

### S21 修复优先级

① 3 处 tv 引入来源（改动最小、防真实样式 bug）→ ② 6 处 emits 类型化 → ③ 表单家族分批 defineModel 改造 → ④ ClassValue 收口随 `createUiProxy` 统一落地。

---

## 补充章节偿还顺序

| 优先级 | 事项 |
|---|---|
| **立即** | S1 密钥吊销与轮换 → 历史清洗（先于一切） |
| **本周** | `git rm --cached .claude/settings.local.json`；S2 重复依赖；S7 Cascader 死代码；S6 Cascader 重复 emits |
| **近期** | S6 Select/Fab defineModel 改造；S13/S14 两个真实 bug；S8 补 40 个 props 中文注释；S18 schema 对齐 + CI 校验；CLAUDE.md 去重补实质内容 |
| **规划** | S3 测试/typecheck/hooks 基建；S11 抽 `createUiProxy`；S10 Fab 拆 composable；S17 DocsAsideLeftBody 重写；S12/S19/S20 清扫 |
