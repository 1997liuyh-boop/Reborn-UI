# 发布与本地调试指南

## 使用 yalc（本地调试神器）

yalc 是目前调试 npm 包的最佳实践，它通过在本地建立一个缓存库，完美避开了软链接（Symlink）在 Windows 和构建工具（Vite/Webpack）中的各种路径报错和卡顿。

### 1. 全局安装 yalc

```bash
npm install -g yalc
```

### 2. 本地发布组件包
在 `reborn-ui` 仓库根目录执行发布：

```bash
yalc publish
```
> **提示**：该命令会迅速把包推送到本地缓存中，速度极快。

### 3. 项目中关联组件包
在 `uniapp-project` 项目根目录执行关联：

```bash
yalc add reborn-ui
```

### 4. 后续开发更新
后续修改了 `reborn-ui` 代码后，只需在组件库目录下执行推送，你的 UniApp 项目就会自动同步更新：

```bash
yalc push
```

---

## CLI 脚手架常用命令

### 1. 验证是否链接到本地 (仅本地调试时)
如果需要确认全局安装的命令是否正确指向本地开发环境，可以在 Windows 终端下使用 `where.exe` 检查：

```bash
where.exe reborn-ui
```

### 2. 初始化项目配置
在项目根目录执行，用于快速基于模板生成基础配置：

```bash
reborn-ui init
```

### 3. 添加组件
快速下载并配置指定的组件（例如添加 `reborn-select`）：

```bash
reborn-ui add reborn-select
```

---

## npm 包发布流程

### 1. 切换目录
进入 cli 目录：
```bash
cd packages/cli
```

### 2. 打包构建
```bash
npm run prepack
```

### 3. 登录 npm 账号
```bash
npm login
```
确认已登录成功：
```bash
npm whoami
```

### 4. 发布至 npm
```bash
npm publish
```
