{
  "$schema": "../schema/component.schema.json",
  "id": "scrollbar",
  "title": "滚动条 Scrollbar",
  "description": "浮层式滚动条组件，隐藏原生滚动条并自绘 thumb，悬浮于内容之上不挤压布局；提供 4/6/8 三档尺寸、轨道显形、圆角内缩与带缓动动画的命令式滚动。",
  "category": "layout",
  "tags": [
    "vue",
    "component",
    "scrollbar",
    "layout"
  ],
  "platforms": [
    "web"
  ],
  "props": [
    {
      "name": "size",
      "type": "number | string",
      "required": false,
      "description": "滚动条粗细，可传 4 / 6 / 8 档位或任意像素数值，默认继承 CSS 变量的 6px",
      "default": "6"
    },
    {
      "name": "always",
      "type": "boolean",
      "required": false,
      "description": "是否常驻显示滚动条；为 false 时仅在悬停容器或滚动过程中淡入",
      "default": "false"
    },
    {
      "name": "minThumbSize",
      "type": "number",
      "required": false,
      "description": "thumb 最小长度（px），防止内容极长时 thumb 小到无法拖拽",
      "default": "20"
    },
    {
      "name": "horizontal",
      "type": "boolean",
      "required": false,
      "description": "是否启用横向滚动条，关闭时横向内容被裁剪",
      "default": "true"
    },
    {
      "name": "hideDelay",
      "type": "number",
      "required": false,
      "description": "停止滚动后隐藏滚动条的延迟（ms），仅 always 为 false 时生效",
      "default": "800"
    },
    {
      "name": "inset",
      "type": "number | \"auto\"",
      "required": false,
      "description": "轨道两端的圆角安全内缩（px）。 \"auto\"（默认）按容器实际 border-radius 自动推算；传数值则固定为该值，传 0 关闭内缩。",
      "default": "\"auto\""
    },
    {
      "name": "track",
      "type": "boolean | string",
      "required": false,
      "description": "轨道是否显形。 false（默认）为完全透明的悬浮感；true 使用内置浅灰轨道并自动适配暗色；传颜色字符串则用该颜色。",
      "default": "false"
    },
    {
      "name": "scrollDuration",
      "type": "number",
      "required": false,
      "description": "命令式滚动（setScrollTop / setScrollLeft）的默认动画时长（ms），传 0 关闭动画",
      "default": "400"
    },
    {
      "name": "wrapClass",
      "type": "string",
      "required": false,
      "description": "附加到内部滚动层的类名，用于设置内边距等",
      "default": "—"
    },
    {
      "name": "contentClass",
      "type": "string",
      "required": false,
      "description": "附加到内容层的类名",
      "default": "—"
    }
  ],
  "events": [
    {
      "name": "scroll",
      "payload": "[payload: { scrollTop: number; scrollLeft: number }]",
      "description": "滚动时触发，携带当前滚动偏移"
    }
  ],
  "slots": [
    {
      "name": "default",
      "description": "滚动区域内容。"
    }
  ],
  "exposes": [
    {
      "name": "update",
      "description": ""
    },
    {
      "name": "refresh",
      "description": ""
    },
    {
      "name": "setScrollTop",
      "description": ""
    },
    {
      "name": "setScrollLeft",
      "description": ""
    },
    {
      "name": "stopScroll",
      "description": ""
    },
    {
      "name": "wrapRef",
      "description": ""
    }
  ],
  "examples": [
    {
      "title": "Web 演示",
      "path": "app/components/reborn/examples/scrollbar/ScrollbarDemo.vue",
      "source": "demo",
      "platform": "web"
    }
  ],
  "related": [],
  "whenToUse": [
    "需要为容器提供统一、细窄、不占宽度的滚动条时。",
    "需要 4 / 6 / 8px 多档尺寸，或按品牌色定制 thumb、轨道与悬停颜色的场景。",
    "需要有 / 无滚动条时内容可视宽度完全一致、避免布局抖动的场景。",
    "需要代码控制滚动位置且要求平滑动画（如目录跳转、卡片轨道翻屏）的场景。",
    "需要随 .dark 自动切换滚动条对比度的深浅色适配场景。"
  ],
  "whenNotToUse": [
    "需要完全隐藏滚动条时，改用 scrollbar-hide 工具类。",
    "整页（html / body）滚动条不适用本组件，请使用站点全局滚动条样式。",
    "需要为滚动条预留占位以避免布局跳动（而非悬浮）时，可自行叠加 scrollbar-gutter: stable。"
  ],
  "pitfalls": [
    "组件需要确定高度才会产生滚动，请在类名上给出 h-* 或由父容器约束高度。",
    "滚动条是自绘 DOM 而非原生滚动条，无法通过 ::-webkit-scrollbar 定制，请改用 --reborn-scrollbar-* 变量或 size / track / inset 属性。",
    "Chromium 下元素的 scrollbar-width / scrollbar-color 为非 auto 值时，所有 ::-webkit-scrollbar 规则整体失效，且 scrollbar-color 会从祖先继承 —— 这是原生滚动条方案无法自定义粗细与悬停色的根因。",
    "overflow: overlay 已在 Chromium 121+ 被移除，computed 回退为 auto，无法实现浮层式滚动条。",
    "容器带 rounded-* 圆角时，根元素的 overflow: hidden 会用圆角弧线裁剪贴边的轨道两端，thumb 滚到顶端 / 末端会被削角。inset 属性默认 'auto'，按容器实际 border-radius 自动推算内缩；传数值可固定内缩，传 0 则完全贴边。",
    "运行时动态改变容器 border-radius 后需在 nextTick() 后调用实例的 refresh() 重算内缩（圆角只改样式不改尺寸，ResizeObserver 不会触发）；仅内容或容器尺寸变化由 ResizeObserver 自动处理。",
    "setScrollTop / setScrollLeft 默认带 easeInOutCubic 缓动并返回 Promise；用户滚轮、触摸、拖拽 thumb 或点击轨道会自动中止动画（Promise 同样 resolve，await 不会挂起）。需要立即落位时传 { animated: false }。"
  ],
  "dependencies": [],
  "install": {
    "cli": "npx reborn-ui@latest add scrollbar"
  },
  "sourceRefs": {
    "web": [
      "app/components/reborn/ui/scrollbar/RebornScrollbar.vue",
      "app/components/reborn/ui/scrollbar/index.ts",
      "app/components/reborn/ui/scrollbar/scrollbar.css"
    ]
  },
  "docPath": "content/2.components/layout/scrollbar.md",
  "_meta": {
    "contentHash": "60d476b4e3b816ee8b9aa522aa3fb9f3f0a15026",
    "hasOverride": true,
    "extractors": {
      "web": "ok",
      "uniapp": "absent",
      "docs": "found"
    }
  }
}
