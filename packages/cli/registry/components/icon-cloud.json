{
  "name": "icon-cloud",
  "title": "图标云",
  "description": "用于以可拖拽旋转的 Canvas 3D 球面展示一组图标的展示组件。",
  "category": "misc",
  "tags": [
    "canvas",
    "interactive",
    "tailwind"
  ],
  "dependencies": [],
  "files": [
    {
      "path": "IconCloud.vue",
      "content": "<script lang=\"ts\" setup>\nimport type { IconCloudProps, SphereIcon } from \"./index\";\nimport { onBeforeUnmount, onMounted, reactive, ref, watch, watchEffect } from \"vue\";\nimport { cn } from \"~/lib/utils\";\n\ninterface TargetRotation {\n  x: number;\n  y: number;\n  startX: number;\n  startY: number;\n  distance: number;\n  startTime: number;\n  duration: number;\n}\n\nconst props = withDefaults(defineProps<IconCloudProps>(), {\n  images: () => [],\n});\n\nconst canvasRef = ref<HTMLCanvasElement | null>(null);\nconst animationFrameRef = ref<number>(0);\n\nconst imageCanvasesRef = ref<HTMLCanvasElement[]>([]);\nconst imagesLoadedRef = ref<boolean[]>([]);\nconst imagePositions = ref<SphereIcon[]>([]);\n\nconst rotation = reactive({ x: 0, y: 0 });\nconst isDragging = ref(false);\nconst lastMousePos = reactive({ x: 0, y: 0 });\nconst mousePos = reactive({ x: 0, y: 0 });\n\nconst targetRotation = ref<TargetRotation | null>(null);\n\nfunction easeOutCubic(t: number): number {\n  return 1 - (1 - t) ** 3;\n}\n\nwatch(\n  () => props.images,\n  (currentImages) => {\n    if (!import.meta.client) {\n      imageCanvasesRef.value = [];\n      imagesLoadedRef.value = [];\n      return;\n    }\n\n    const images = currentImages ?? [];\n    imagesLoadedRef.value = new Array(images.length).fill(false);\n\n    const newImageCanvases = images.map((url, idx) => {\n      const offscreen = document.createElement(\"canvas\");\n      offscreen.width = 40;\n      offscreen.height = 40;\n\n      const offCtxRaw = offscreen.getContext(\"2d\");\n      if (!offCtxRaw) {\n        return offscreen;\n      }\n\n      const offCtx: CanvasRenderingContext2D = offCtxRaw;\n      const img = new Image();\n      img.crossOrigin = \"anonymous\";\n      img.src = url;\n      img.onload = () => {\n        offCtx.clearRect(0, 0, offscreen.width, offscreen.height);\n        offCtx.beginPath();\n        offCtx.arc(20, 20, 20, 0, Math.PI * 2);\n        offCtx.closePath();\n        offCtx.clip();\n        offCtx.drawImage(img, 0, 0, 40, 40);\n\n        if (idx < imagesLoadedRef.value.length) {\n          imagesLoadedRef.value[idx] = true;\n        }\n      };\n\n      return offscreen;\n    });\n\n    imageCanvasesRef.value = newImageCanvases;\n  },\n  {\n    immediate: true,\n  },\n);\n\nwatchEffect(() => {\n  const count = props.images.length;\n  if (count === 0) {\n    imagePositions.value = [];\n    return;\n  }\n\n  const newPositions: SphereIcon[] = [];\n  const offset = 2 / count;\n  const increment = Math.PI * (3 - Math.sqrt(5));\n\n  for (let i = 0; i < count; i++) {\n    const y = i * offset - 1 + offset / 2;\n    const r = Math.sqrt(1 - y * y);\n    const phi = i * increment;\n    const x = Math.cos(phi) * r;\n    const z = Math.sin(phi) * r;\n\n    newPositions.push({\n      x: x * 100,\n      y: y * 100,\n      z: z * 100,\n      scale: 1,\n      opacity: 1,\n      id: i,\n    });\n  }\n\n  imagePositions.value = newPositions;\n});\n\nfunction handleMouseDown(e: MouseEvent) {\n  const canvas = canvasRef.value;\n  if (!canvas) return;\n\n  const rect = canvas.getBoundingClientRect();\n  const x = e.clientX - rect.left;\n  const y = e.clientY - rect.top;\n\n  imagePositions.value.forEach((icon) => {\n    const cosX = Math.cos(rotation.x);\n    const sinX = Math.sin(rotation.x);\n    const cosY = Math.cos(rotation.y);\n    const sinY = Math.sin(rotation.y);\n\n    const rotatedX = icon.x * cosY - icon.z * sinY;\n    const rotatedZ = icon.x * sinY + icon.z * cosY;\n    const rotatedY = icon.y * cosX + rotatedZ * sinX;\n\n    const screenX = canvas.width / 2 + rotatedX;\n    const screenY = canvas.height / 2 + rotatedY;\n    const scale = (rotatedZ + 200) / 300;\n    const radius = 20 * scale;\n    const dx = x - screenX;\n    const dy = y - screenY;\n\n    if (dx * dx + dy * dy < radius * radius) {\n      const targetX = -Math.atan2(icon.y, Math.sqrt(icon.x * icon.x + icon.z * icon.z));\n      const targetY = Math.atan2(icon.x, icon.z);\n      const currentX = rotation.x;\n      const currentY = rotation.y;\n      const distance = Math.sqrt((targetX - currentX) ** 2 + (targetY - currentY) ** 2);\n      const duration = Math.min(2000, Math.max(800, distance * 1000));\n\n      targetRotation.value = {\n        x: targetX,\n        y: targetY,\n        startX: currentX,\n        startY: currentY,\n        distance,\n        startTime: performance.now(),\n        duration,\n      };\n    }\n  });\n\n  isDragging.value = true;\n  lastMousePos.x = e.clientX;\n  lastMousePos.y = e.clientY;\n}\n\nfunction handleMouseMove(e: MouseEvent) {\n  const canvas = canvasRef.value;\n  if (!canvas) return;\n\n  const rect = canvas.getBoundingClientRect();\n  mousePos.x = e.clientX - rect.left;\n  mousePos.y = e.clientY - rect.top;\n\n  if (isDragging.value) {\n    const deltaX = e.clientX - lastMousePos.x;\n    const deltaY = e.clientY - lastMousePos.y;\n\n    rotation.x += deltaY * 0.002;\n    rotation.y += deltaX * 0.002;\n\n    lastMousePos.x = e.clientX;\n    lastMousePos.y = e.clientY;\n  }\n}\n\nfunction handleMouseUp() {\n  isDragging.value = false;\n}\n\nonMounted(() => {\n  const canvas = canvasRef.value;\n  if (!canvas) return;\n\n  const ctxRaw = canvas.getContext(\"2d\");\n  if (!ctxRaw) return;\n\n  const ctx: CanvasRenderingContext2D = ctxRaw;\n  const safeCanvas: HTMLCanvasElement = canvas;\n\n  function animate() {\n    ctx.clearRect(0, 0, safeCanvas.width, safeCanvas.height);\n\n    const centerX = safeCanvas.width / 2;\n    const centerY = safeCanvas.height / 2;\n    const dx = mousePos.x - centerX;\n    const dy = mousePos.y - centerY;\n    const maxDistance = Math.sqrt(centerX * centerX + centerY * centerY);\n    const distance = Math.sqrt(dx * dx + dy * dy);\n    const speed = 0.003 + (distance / maxDistance) * 0.01;\n\n    if (targetRotation.value) {\n      const { startX, startY, x: tx, y: ty, startTime, duration } = targetRotation.value;\n      const elapsed = performance.now() - startTime;\n      const progress = Math.min(1, elapsed / duration);\n      const eased = easeOutCubic(progress);\n\n      rotation.x = startX + (tx - startX) * eased;\n      rotation.y = startY + (ty - startY) * eased;\n\n      if (progress >= 1) {\n        targetRotation.value = null;\n      }\n    } else if (!isDragging.value) {\n      rotation.x += (dy / safeCanvas.height) * speed;\n      rotation.y += (dx / safeCanvas.width) * speed;\n    }\n\n    imagePositions.value.forEach((icon, index) => {\n      const cosX = Math.cos(rotation.x);\n      const sinX = Math.sin(rotation.x);\n      const cosY = Math.cos(rotation.y);\n      const sinY = Math.sin(rotation.y);\n\n      const rotatedX = icon.x * cosY - icon.z * sinY;\n      const rotatedZ = icon.x * sinY + icon.z * cosY;\n      const rotatedY = icon.y * cosX + rotatedZ * sinX;\n\n      const scale = (rotatedZ + 200) / 300;\n      const opacity = Math.max(0.2, Math.min(1, (rotatedZ + 150) / 200));\n      const imageCanvas = imageCanvasesRef.value[index];\n      const isLoaded = imagesLoadedRef.value[index] === true;\n\n      ctx.save();\n      ctx.translate(centerX + rotatedX, centerY + rotatedY);\n      ctx.scale(scale, scale);\n      ctx.globalAlpha = opacity;\n\n      if (imageCanvas && isLoaded) {\n        ctx.drawImage(imageCanvas, -20, -20, 40, 40);\n      }\n\n      ctx.restore();\n    });\n\n    animationFrameRef.value = requestAnimationFrame(animate);\n  }\n\n  animationFrameRef.value = requestAnimationFrame(animate);\n});\n\nonBeforeUnmount(() => {\n  if (animationFrameRef.value) {\n    cancelAnimationFrame(animationFrameRef.value);\n  }\n});\n</script>\n\n<template>\n  <canvas\n    ref=\"canvasRef\"\n    width=\"300\"\n    height=\"300\"\n    role=\"img\"\n    aria-label=\"Interactive 3D Image Cloud\"\n    :class=\"cn('rounded-lg', props.class)\"\n    @mousedown=\"handleMouseDown\"\n    @mousemove=\"handleMouseMove\"\n    @mouseup=\"handleMouseUp\"\n    @mouseleave=\"handleMouseUp\"\n  />\n</template>\n",
      "target": "web"
    },
    {
      "path": "index.ts",
      "content": "import type { HTMLAttributes } from \"vue\";\n\nexport interface SphereIcon {\n    x: number;\n    y: number;\n    z: number;\n    scale: number;\n    opacity: number;\n    id: number;\n}\n\nexport interface IconCloudProps {\n    class?: HTMLAttributes[\"class\"];\n    images?: string[];\n}\n\nexport { default as IconCloud } from \"./IconCloud.vue\";\n\n"
    }
  ],
  "fileCount": 2,
  "contentHash": "7aa809060d9a09e80cd0222da5ee45d3d2bfbd06"
}
