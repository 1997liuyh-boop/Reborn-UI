export default {
  slots: {
    placeholder: 'w-full',
    wrapper: 'fixed bottom-0 left-0 z-40 w-full overflow-visible',
    base: 'overflow-visible pb-[env(safe-area-inset-bottom)]',
    content: 'overflow-visible px-3 py-3',
  },
} as const
