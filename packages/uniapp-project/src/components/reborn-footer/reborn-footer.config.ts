export default {
  slots: {
    placeholder: 'w-full',
    wrapper: 'fixed bottom-0 left-0 z-30 w-full overflow-visible',
    footer: 'reborn-footer overflow-visible bg-white pb-[env(safe-area-inset-bottom)] dark:bg-gray-9',
    content: 'overflow-visible px-3 py-3',
  },
} as const
