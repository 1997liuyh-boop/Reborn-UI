const config = {
    slots: {
        wrapper: "w-full h-full",
        header: "flex flex-row items-center py-4 reborn-picker-view__header px-4 absolute top-0 w-full z-10 bg-white dark:bg-gray-8 border-b border-gray-1 dark:border-gray-7",
        headerText: "flex-1 text-center font-medium text-gray-8 dark:text-gray-2",
        pickerContainer: "px-1 relative mt-[50px]",
        item: "flex flex-row items-center justify-center reborn-picker-view__item w-full",
        itemText: "transition-colors duration-200",
    },
    variants: {},
    defaultVariants: {}
};

export default config;