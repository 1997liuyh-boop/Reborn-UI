# 样式令牌收口清单

> 本轮只出清单，不改组件代码。规则部分同步写在 `AGENTS.md` 的「样式令牌规范」小节。

## 零、方案方向：覆盖原生档，不是删除

最初的方案是「删掉 `--radius-ui-*`，让组件投降到 Tailwind 原生档」。**改了**，现在的方案是**保留这六个值，把它们写进原生档的名字下**。

原因在 `app/assets/theme/typography.css:5` 的注释里：

> Web / PC 端基础字号阶梯（7 级），**覆盖 Tailwind 原生 `--text-*` 默认值**。

字号走的是「覆盖原生档」，圆角走的是「另起 `ui-` 命名空间」。**同一套设计体系，两种令牌策略——这才是不统一的根源**，圆角是那个不合群的决定。

直接删会把问题换一种形式留下：类名统一了，但圆角变 rem、字号仍是 px，根字号一改圆角缩放、字号不动，从「写法不一致」变成更隐蔽的「行为不一致」。

### 两个前提数据

| 事实 | 数 | 影响 |
|---|---|---|
| Web 侧 `rounded-ui-*` 用量 | 141 文件 / 342 处 | 类名迁移工作量，两个方案都一样 |
| Web 侧 `var(--radius-ui-*)` 直接引用 | **0** | 改名安全，没有手写 CSS 依赖旧名 |
| uniapp 侧原生 `rounded-*` 用量 | **248 处** | uniapp 不能跟着覆盖，见第五节 |
| 全仓 `text-xs` 用量 | **369 处** | 堵死了「补 10px 令牌」的路，见第三节 |

---

## 一、圆角：覆盖原生档

### 改 `app/assets/theme/base.css:97-103`

把现有六行换成：

```css
/* 圆角 (Radius) —— 覆盖 Tailwind 原生 --radius-*，与字号同样锁定 px */
--radius-sm: 4px;    /* 原生 0.25rem */
--radius-md: 6px;    /* 原生 0.375rem */
--radius-lg: 8px;    /* 原生 0.5rem */
--radius-xl: 12px;   /* 原生 0.75rem */
--radius-2xl: 16px;  /* 原生 1rem */
--radius-3xl: 24px;  /* 原生 1.5rem */
```

`--radius-xs`(2px) 与 `--radius-4xl`(32px) 不在本体系内，保持原生值不覆盖，写法比照 typography.css:31 的处理。

**视觉零变化**：选的 px 值正好等于原生 rem 值在 16px 根字号下的计算结果，逐档对应。所以覆盖之后现有原生 `rounded-*` 用法一个像素都不动，只是从「随根字号漂移」锁成「固定像素」——这正是与字号令牌行为对齐的目的。

### 类名映射（342 处照此替换）

| 作废 | 改用 | 值 |
|---|---|---|
| `rounded-ui-2xs` | `rounded-sm` | 4px |
| `rounded-ui-xs` | `rounded-md` | 6px |
| `rounded-ui-sm` | `rounded-lg` | 8px |
| `rounded-ui-md` | `rounded-xl` | 12px |
| `rounded-ui-base` | `rounded-2xl` | 16px |
| `rounded-ui-lg` | `rounded-3xl` | 24px |

方向性变体替换中段：`rounded-t-ui-xs` → `rounded-t-md`，`rounded-s-ui-sm` → `rounded-s-lg`，依此类推。

裸 `rounded` 是硬编码 4px 字面量、不读任何令牌，遇到时一并换成 `rounded-sm`。

### 分布

| 范围 | 文件 | 处数 | 处置 |
|---|---|---|---|
| `app/components/reborn/ui` | 31 | 107 | 迁移 |
| `app/components/reborn/examples` | 85 | 191 | 迁移 |
| `app/components/common` | 3 | 6 | 迁移 |
| `app/lib/floating.ts` | 1 | 1 | 迁移 |
| `content/`（文档正文与令牌表） | 18 | 36 | 改文档 |
| `knowledge/overrides/` | 3 | 1 | 改 1 处 |
| **Web 侧合计** | **141** | **342** | |
| `packages/uniapp-project/src` | 18 | 82 | **不动，见第五节** |
| `knowledge/components/` | 3 | — | 生成物，`kb:build` 自动跟随 |

按 token：`ui-sm` 89 · `ui-md` 64 · `ui-2xs` 50 · `ui-xs` 33 · `ui-lg` 29 · `ui-base` 23（无方向共 288），方向变体 54。

### 组件源码逐文件（31 文件 / 107 处）

| 文件 | 处数 |
|---|---|
| `reborn-tabs/reborn-tabs.config.ts` | 25 |
| `reborn-search-box/reborn-search-box.config.ts` | 9 |
| `reborn-radio/reborn-radio.config.ts` | 8 |
| `reborn-select/reborn-select.config.ts` | 5 |
| `reborn-button/reborn-button.config.ts` | 4 |
| `reborn-switch` / `reborn-input` / `reborn-input-number` 的 config | 各 3 |
| `reborn-tree` `reborn-select-trigger` `reborn-layout` `reborn-guide` `reborn-field-trigger` `reborn-dropdown` `reborn-context-menu` `reborn-checkbox` `reborn-cascader` 的 config | 各 2 |
| `reborn-tooltip` `reborn-toast` `reborn-slider` `reborn-popover`(config+vue) `reborn-notification` `reborn-notice-bar` `reborn-menu` `reborn-dialog` `reborn-carousel` `reborn-breadcrumb` `reborn-badge` `reborn-alert`、`RebornSearchBox.vue` | 各 1 |

`reborn-tabs` 一个文件占四分之一，单独一个提交。

**优先处理这两个文件**（同文件内混用双轨，最容易读错值）：`reborn-context-menu.config.ts`、`reborn-guide.config.ts`。

### 文档侧（18 文件 / 36 处）

- `content/1.getting-started/5.tailwind4.md:119-124` —— 令牌说明表，逐行写了变量名与像素值，连说明文字一起改
- `content/2.components/button/reborn-button.md:116-118` —— web 侧令牌表，改
- `content/2.components/button/reborn-button.md:127-133` —— **uniapp 侧 rpx 表，不改**（见第五节）
- 其余 16 文件是代码示例里的类名替换

### 收尾

- 全仓扫 `rounded-ui-`：Web 侧**活类名**应归零（注释/文档里的历史说明不计）；uniapp 侧不改动，以 `git status` 为准而非计数
- 扫 `var(--radius-ui-`：Web 侧 0（已验证），uniapp 侧保持 1 处（`theme.css:138`）
- 跑 `pnpm kb:build`

> **这两条当初写错了，收尾时按实测改写：**
> 1. 「Web 侧应归零」不成立——迁移本身会在注释和文档里留下「原先用 `rounded-ui-*`」这类**历史表述**，扫描必然非零。正确判据是活类名归零，见下方验收扫描。
> 2. 「uniapp 侧应保持 82」这个数复现不出来。同一份代码四种量法各不相同：含该串的**行数** 80、裸前缀 `rounded-ui-` **出现次数** 140、算上方向变体（`rounded-t-ui-xs` 等）的**完整类名次数** 308、**涉及文件数** 55。清单里的 82 和过程中记的 46 都无法用任何一种量法复现，属于当初的计数口径错误。
>    uniapp 是否被误改，计数本来就不是判据——`git status packages/uniapp-project` 才是：该包只有 `pages.json`、`pages/index/index.vue` 两个改动共 14 行插入，内容是注册新增的 `reborn-empty` / `reborn-result` 页面，与圆角无关；其余均为未跟踪的新增目录。**本轮未触碰 uniapp 一行圆角代码**，这是确定的。

---

## 二、字号：有令牌却写了字面量（22 处 / 8 文件，已做）

替换规则：`text-[12px]`→`text-sm`、`text-[14px]`→`text-base`、`text-[16px]`→`text-lg`、`text-[24px]`→`text-2xl`。

**不是纯等价替换。** 这些位置多数同时写了 `leading-[1.5]` / `leading-[1.35]` / `leading-none`。令牌自带 `--text-*--line-height`（行高 = 字号 + 8px），替换后应把 `leading-*` 一并删掉，否则只用了令牌的一半。行高会变——`text-[14px] leading-[1.5]` 是 21px，`text-base` 是 22px，差 1px。**逐个过视觉。**

| 文件:行 | 现值 | 改为 | 位置 |
|---|---|---|---|
| `reborn-chip.config.ts:28` | `text-[12px]` | `text-sm` | xl 档 label |
| `reborn-dialog/index.ts:282` | `text-[16px]` | `text-lg` | 命令式标题 |
| `reborn-dialog.config.ts:27` | `text-[16px]` | `text-lg` | title |
| `reborn-dialog.config.ts:29` | `text-[14px]` | `text-base` | description |
| `reborn-dropdown.config.ts:111` | `text-[12px]` | `text-sm` | groupTitle |
| `reborn-progress.config.ts:44` | `text-[14px]` | `text-base` | text |
| `reborn-progress.config.ts:81` | `text-[14px]` | `text-base` | circle sm |
| `reborn-progress.config.ts:82` | `text-[16px]` | `text-lg` | circle md |
| `reborn-progress.config.ts:83` | `text-[24px]` | `text-2xl` | circle lg |
| `reborn-radio.config.ts:97` | `text-[12px]` | `text-sm` | sm label |
| `reborn-radio.config.ts:98` | `text-[14px]` | `text-base` | md label |
| `reborn-radio.config.ts:99` | `text-[16px]` | `text-lg` | lg label |
| `reborn-select.config.ts:84` | `text-[14px]` | `text-base` | empty |
| `reborn-select.config.ts:103` | `text-[14px]` | `text-base` | 加载态 |
| `reborn-select.config.ts:193` | `text-[12px]` | `text-sm` | sm trigger |
| `reborn-select.config.ts:200` | `text-[14px]` | `text-base` | md trigger |
| `reborn-select.config.ts:202` | `text-[12px]` | `text-sm` | md tag |
| `reborn-select.config.ts:206` | `text-[16px]` | `text-lg` | lg trigger |
| `reborn-select.config.ts:208` | `text-[14px]` | `text-base` | lg tag |
| `reborn-switch.config.ts:155` | `text-[12px]` | `text-sm` | lg 内嵌文字 ×2 |
| `reborn-select.config.ts:39` | `text-[14px]` | `text-base` | trigger 基线（**清单漏列，执行时补上**） |

### 执行时的四处偏离

1. **`reborn-progress.config.ts` 的 `leading-none` 保留，没删。** `root` 是 `inline-flex items-center`，谁高谁撑高整条进度条；换成令牌自带的 22px 行高，line 型进度条会平白长高 8px。这一处只换字号、行高照旧。
2. **多删了两处 `leading-[1.5]`**：`reborn-select.config.ts` 的 `option`、`reborn-dropdown.config.ts` 的 `item`。两者原本是 `text-base leading-[1.5]`，不在清单里。但它们的同层兄弟（header / footer / groupTitle）按 C 组要删 `leading-[1.5]`，只删兄弟会让同一个下拉面板里的 14px 文字有的 22px 行高、有的 21px——那是我自己的改动造出来的新不一致。行高 21→22px，`max-h-60` 内单行由 29px 变 30px。
3. **`reborn-select.config.ts` 原注释是错的，一并改写。** 原注释称「不用 text-sm/base/lg，那三个 token 自带的固定行高会把 `leading-[1.5]` 覆盖掉」。事实相反：Tailwind 把 `leading-*` 排在 `text-*` 之后，显式 `leading-*` 赢；tailwind-merge 也保留两者（不同组）。同文件第 71 行的注释还与它自相矛盾。已按实际行为重写。
4. **清理了 14 行被本轮改动写失效的旧注释（8 文件，清单里没有这一步）。** 本轮删掉 `--radius-ui-*`、类名全换原生 `rounded-*` 之后，源码与文档里 11 处描述 `rounded-ui-*` 的说明**当场变成假话**——是我的改动造成的，不是历史欠债，所以就地修掉而不是留给下一个人。改的是 6 个 config 的注释、3 个 `.md`、2 个 `knowledge/overrides`。其中两处不只是过时，而是**在教人做错事**：`knowledge/overrides/reborn-input-number.json:21` 和 `content/2.components/input-and-forms/reborn-select.md:546` 原文都让读者覆盖圆角时**补 `!`**，迁移后这条建议已经不成立。

   这一步顺带查清了 `!` 到底为什么存在——读 `app/lib/utils.ts` 确认 `extendTailwindMerge` **只**扩了 `font-size` 和 `px` 两个冲突组，`rounded-ui-*` 从来没注册进去。所以当年两个圆角类会共存、由 CSS 源序定胜负，`!` 是**承重**的；现在换成原生 `rounded-*`，tailwind-merge 能正常合并、后写者胜出，`!` 只剩提权作用。

   **但 `rounded-*!` 里的 `!` 一处都没删。** 它现在冗余，却不是无害到可以顺手清理：tailwind-merge 的「后写者胜出」依赖 tv 的声明顺序，我没有逐个 variant 验证过那个顺序。留着 `!` 不改变任何渲染结果，删掉才是行为变更，该单独立项做回归，不该混在本轮里。

   两个连带发现：
   - `reborn-select.config.ts:132` 的 `!` **仍然必需**，不能跟着一起注销。那行同时管高度和圆角，而 Badge 的档位走自定义的 `h-badge-*`（`typography.css:48-50`），至今**没**注册进 tailwind-merge 冲突组。差点被我一并改写，核对后拆成两段分别说明。
   - `reborn-select.config.ts:180` 原注释里「`px-input-px-*` 不在冲突组」这句**在本轮之前就是错的**——`utils.ts` 明确把它注册进了 `px` 组。我正好在重写这条注释，照抄就等于自己签发一条新的假话，故一并按实际改对。

---

## 三、字号：规范外的 21 处

### 前置发现：`text-xs` 是个已存在的坑

| 目录 | `text-xs` 处数 |
|---|---|
| `app/components/reborn/examples` | 278 |
| `app/components/reborn/ui` | **42** |
| `app/components/common` | 26 |
| `content` | 18 |
| `app/pages` | 5 |
| **合计** | **369** |

两件事：

1. **补 10px 令牌的路被堵死。** 本来想覆盖 `--text-xs` 为 10px 来收编下面的 B 组，369 处用量下不可行，放弃。
2. **组件源码里那 42 处本身违规。** typography.css:31 写着「`text-xs` 不属于本规范，仅文档站与落地页可用，reborn 组件一律从 7 级中取」，组件里却有 42 处。而且原生 `text-xs` = 0.75rem = 12px，项目 `--text-sm` = 12px——**两者在默认根字号下同值，一个 rem 一个 px**，看起来一样大、行为不同。这是本轮之外的另一笔账，单独记着。

### A 组｜档位联动（9 处）——**不动**

字号绑在组件尺寸档上，跟着容器高度按比例缩，不是独立字级，进不了 7 级体系。就地保留字面量，config 里补中文注释注明理由。

| 文件:行 | 值 | 容器 |
|---|---|---|
| `reborn-chip.config.ts:24` | `text-[4px]` | `h-[10px]` xs |
| `reborn-chip.config.ts:25` | `text-[6px]` | `h-[12px]` sm |
| `reborn-chip.config.ts:26` | `text-[8px]` | `h-[14px]` md |
| `reborn-chip.config.ts:27` | `text-[10px]` | `h-[16px]` lg |
| `reborn-switch.config.ts:153` | `text-[8px]` | sm 内嵌文字 |
| `reborn-switch.config.ts:154` | `text-[10px]` | md 内嵌文字 |
| `reborn-transfer.config.ts:203` | `text-[9px]` | sm 操作按钮 |
| `reborn-transfer.config.ts:225` | `text-[10px]` | md 操作按钮 |
| `reborn-transfer.config.ts:247` | `text-[11px]` | lg 操作按钮 |

> `reborn-chip` 的 `text-[4px]` / `text-[6px]` 待人工确认渲染效果：浏览器普遍有最小字号下限，中文在 4px 下基本不可读，实际表现可能不是设计意图。这两处的结论待定。

### B 组｜独立小字（8 处）——**不动**，补注释

不随档位变的固定小字。补令牌的路被 `text-xs` 堵死，为 8 处新开一级不划算。就地保留，config 里注明理由。

| 文件:行 | 值 | 位置 |
|---|---|---|
| `reborn-carousel.config.ts:204` | `text-[10px]` | 指示器数字（容器 `size-5`） |
| `reborn-color-picker-panel.config.ts:17` | `text-[10px]` | presetTitle |
| `RebornColorPickerPanel.vue:274` | `text-[10px]` | 格式切换按钮 |
| `reborn-progress.config.ts:85` | `text-[10px]` | 进度条内嵌文字 |
| `reborn-select.config.ts:196` | `text-[10px]` | sm 档 tag（容器 `h-4!`） |
| `reborn-transfer.config.ts:188` | `text-[10px]` | panelCount |
| `reborn-transfer.config.ts:199` | `text-[10px]` | itemDesc |
| `reborn-context-menu.config.ts:31` | `text-[11px]` | itemKbd 快捷键标签 |

### C 组｜13px（4 处）——**改 `text-sm`**

同一视觉模式（下拉面板页眉页脚），字符串逐字相同，是复制产物。落 12px：这是面板附属信息，比选项正文（14px）轻一级合理。

| 文件:行 | 现值 |
|---|---|
| `reborn-dropdown.config.ts:102` | `px-[10px] py-[6px] text-[13px] leading-[1.5] text-gray-6`（header） |
| `reborn-dropdown.config.ts:112` | 同上（footer） |
| `reborn-select.config.ts:98` | 同上（header） |
| `reborn-select.config.ts:100` | 同上（footer） |

四处一起动，`leading-[1.5]` 一并删掉（`text-sm` 自带 20px 行高）。

---

## 四、间距：本轮不动

组件源码里刻度写法 580 处、字面量 146 处。字面量中约 136 处能落到刻度（`4px→1`、`8px→2`、`12px→3`、`16px→4`、`24px→6`，半档 `2px→0.5`、`6px→1.5`、`10px→2.5`），`3/5/7/26/27px` 约 10 处落不上。

不动的理由：刻度是 rem、字面量是 px，替换会把这 136 处从固定像素变成随根字号缩放。要么接受这个变化，要么连整套 `--spacing` 一起 px 化——后者是全仓级改动，收益不抵风险。**只在 AGENTS.md 立规矩管新代码，存量留着。**

> 若日后决定统一：正确做法是比照圆角，在 `@theme` 里覆盖 `--spacing` 为 px，而不是把 136 处字面量改成刻度类名。

---

## 五、uniapp：本轮不碰，另案

`packages/uniapp-project/src/styles/theme.css:66-71` 有独立的一份 `--radius-ui-*`，值是 rpx；第 138 行 `--radius: var(--radius-ui-md)` 依赖它。

本来考虑两端同步覆盖，让 `rounded-lg` 在 Web 是 8px、在 uniapp 是 8rpx，名字对齐。**否掉了**：uniapp 端现有 **248 处**原生 `rounded-*`（rem），覆盖成 rpx 后圆角在 375 屏上减半，248 处全要回归验证。不是顺手能干的量。

**代价要说清楚：两端类名暂时分家。** Web 写 `rounded-lg`，uniapp 继续写 `rounded-ui-sm`。这与「跨端一致性」原则冲突，是本方案已知的欠账。

uniapp 端真正的问题不是命名，是 248 处 rem 与 82 处 rpx 混用——rem 在小程序里不随设计稿缩放，按项目「尺寸优先 rpx」的规则，那 248 处本身就是 bug。单独立项处理，届时一并对齐命名。

---

## 六、执行顺序

| # | 内容 | 规模 | 状态 |
|---|---|---|---|
| 1 | 改 `base.css:97-103` 为覆盖原生档 | 6 行 | 已完成 |
| 2 | 第二节 22 处字面量字号（逐个看行高） | 8 文件 | 已完成 |
| 3 | 第三节 C 组 4 处 13px → `text-sm` | 2 文件 | 已完成 |
| 4 | 圆角：`reborn-tabs` 单独一提交 | 25 处 | 已完成 |
| 5 | 圆角：其余 30 个组件文件 | 82 处 | 已完成 |
| 6 | 圆角：examples | 85 文件 / 191 处 | 已完成 |
| 7 | 圆角：common + `app/lib/floating.ts` | 4 文件 / 7 处 | 已完成 |
| 8 | 圆角：文档 18 文件（`reborn-button.md:127-133` 除外） | 36 处 | 已完成 |
| 9 | 圆角：`knowledge/overrides` 1 处 | 1 处 | 已完成 |
| 10 | A / B 组补中文注释 | 17 处 | 已完成 |

验收扫描：

- `app/components/reborn/ui` 内 `text-[Npx]` 余 17 处，全部 ≤ 11px，确实在 `--text-sm: 12px` 下限之外，逐处有中文注释。
- `rounded-ui-` Web 侧**活类名 0 处**：针对引号内 / `class` 属性内类名的定向正则返回 0。全仓扫该字符串仍有 10 处命中，逐条核对过，全部是我在注释与文档里写的历史表述（「size 轴原先用自定义的 `rounded-ui-*`」），不是可生效的类名。
- `var(--radius-ui-`：Web 侧 0，uniapp 侧 1 处（`theme.css:138`），与预期一致。

每步之后 `npx eslint <改动路径>`（`prettier` / `pnpm lint` 基线是红的，不作门禁；uniapp 包内严禁 `--fix`）。第 9 步后跑 `pnpm kb:build`。提交前按 CLAUDE.md 跑 `detect_changes({scope: "all"})`。

### 本轮之外记账

- 组件源码 42 处 `text-xs` 违反 typography.css:31 自己的规范
- uniapp 端 248 处 rem 圆角 + 两端命名分家
- 间距 136 处 px/rem 双轨
- **`text-{令牌} leading-[字面量]` 半迁移写法，`app/components/reborn/ui` 内余 18 处**（alert / anchor / breadcrumb / button×3 / context-menu×2 / dialog-index / empty×2 / popconfirm×2 / result×2 / toast / tooltip / LetterPullup）。字号已走令牌、行高仍是字面量，属于「只用了令牌一半」的同一类问题，但不在本轮清单内，未动。其中 `reborn-popconfirm.config.ts` 的 `text-base leading-[22px]` 与令牌值完全相等，删掉即可；`leading-[1.5]` 系列删掉是 ±1px；`LetterPullup.vue` 的 `text-4xl leading-[5rem]` 是刻意的展示排版，不该删
- 本轮新发现两处 `text-xs`：`reborn-context-menu.config.ts:29`（itemDescription）、`reborn-color-picker-panel.config.ts:15`（input），已计入上面 42 处那笔账
